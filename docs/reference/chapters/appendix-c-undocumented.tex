\chapter{Undocumented Instructions}
\label{appendix:undocumented}

\section{Overview}

The SIRCIS instruction set includes 14 undocumented instruction opcodes. Most of these opcodes are valid encodings with
architecturally undefined behaviour: software must not depend on their results, side effects, timing, flag behaviour,
or fault behaviour. Two of the fourteen, \opcode{27} and \opcode{2F}, are the exception -- each has fully defined,
tested behaviour (\opcode{27}: \texttt{rD} is loaded with the zero-extended 8-bit immediate; \opcode{2F}: the
coprocessor command is the 8-bit immediate and the coprocessor ID is always 0, so the instruction is never privileged)
but no public assembly syntax, and they are listed here for that reason rather than because their behaviour is
undefined. Undocumented opcodes are reserved for future CPU revisions and may be repurposed without preserving their
current simulator or hardware behaviour.

\textbf{Warning:} Use of undocumented instructions is not recommended for production code.

\section{Undefined Operand Combinations}

Undefined operand combinations are different from undocumented opcodes: the opcode is documented, but a particular
choice of operands creates multiple architectural writes to the same address-register pair during the same instruction.

\subsection{Aliased Address-Register Writes}

The auto-update effective-address forms can write both a destination address-register pair and an updated source
address-register pair. \mnemonic{LDEL} also writes the link-register pair. If two of those write-back targets are the
same address-register pair, the final value is architecturally undefined.

\begin{lstlisting}
; Undefined: a is both destination and auto-updated source
LDEA a, -(#0, a)

; Undefined: l is both link register and auto-updated source
LDEL p, (#0, l)+

; Undefined: l is both link register and destination
LDEL l, (#0, a)
\end{lstlisting}

Programs should avoid aliased register writes. The assembler rejects the known hazardous forms so that programs do not
accidentally depend on undefined write-back ordering.

\section{Undocumented Opcode List}

\begin{table}[H]
	\centering
	\begin{tabular}{clll}
		\toprule
		\textbf{Opcode} & \textbf{Format} & \textbf{Pattern}        & \textbf{Notes}            \\
		\midrule
		\opcode{08}     & Immediate       & ADD-like + \texttt{0x8} & Test variant (no save)    \\
		\opcode{09}     & Immediate       & ADC-like + \texttt{0x8} & Test variant (no save)    \\
		\opcode{0B}     & Immediate       & SBC-like + \texttt{0x8} & Test variant (no save)    \\
		\opcode{0D}     & Immediate       & OR-like + \texttt{0x8}  & Test variant (no save)    \\
		\opcode{27}     & Short Imm       & LOAD-like               & No public assembly syntax \\
		\opcode{28}     & Short Imm       & ADD-like + \texttt{0x8} & Test variant (no save)    \\
		\opcode{29}     & Short Imm       & ADC-like + \texttt{0x8} & Test variant (no save)    \\
		\opcode{2B}     & Short Imm       & SBC-like + \texttt{0x8} & Test variant (no save)    \\
		\opcode{2D}     & Short Imm       & OR-like + \texttt{0x8}  & Test variant (no save)    \\
		\opcode{2F}     & Short Imm       & COP-like                & No public assembly syntax \\
		\opcode{38}     & Register        & ADD-like + \texttt{0x8} & Test variant (no save)    \\
		\opcode{39}     & Register        & ADC-like + \texttt{0x8} & Test variant (no save)    \\
		\opcode{3B}     & Register        & SBC-like + \texttt{0x8} & Test variant (no save)    \\
		\opcode{3D}     & Register        & OR-like + \texttt{0x8}  & Test variant (no save)    \\
		\bottomrule
	\end{tabular}
	\caption{Undocumented Instruction Opcodes}
\end{table}

\section{Observed Behavior}

The undocumented opcodes with bit 3 set in the ALU operation field compute \texttt{alu\_code \& 0x7} as the operation,
discard the result, and update all four flags exactly as the corresponding documented operation does. This is the
observed behavior of the current reference implementation; it is architecturally undefined, so software must not rely
on it.

\subsection{\opcode{08}, \opcode{28}, \opcode{38} (ADD Test)}

Computes the sum of the two operands but discards the result, updating flags as an ADD would. This is comparable to
\mnemonic{CMP}, but using ADD semantics instead of SUB.

\begin{lstlisting}
result = operand1 + operand2  ; Calculate
; result is discarded (not written to register)
; Flags updated based on result
\end{lstlisting}

\textbf{Utility:} Limited. \mnemonic{CMP} already provides comparison functionality.

\subsection{\opcode{09}, \opcode{29}, \opcode{39} (ADC Test)}

Computes the sum of the two operands plus the incoming carry, but discards the result, updating flags.

\begin{lstlisting}
result = operand1 + operand2 + C
; result is discarded
; Flags updated
\end{lstlisting}

\textbf{Utility:} Possibly useful for testing carry propagation without modifying registers.

\subsection{\opcode{0B}, \opcode{2B}, \opcode{3B} (SBC Test)}

Computes the difference of the two operands minus the incoming borrow, but discards the result, updating flags.

\begin{lstlisting}
result = operand1 - operand2 - C  ; C is the borrow input
; result is discarded
; Flags updated
\end{lstlisting}

\textbf{Utility:} Possibly useful for testing borrow propagation in multi-precision arithmetic.

\subsection{\opcode{0D}, \opcode{2D}, \opcode{3D} (OR Test)}

Computes the bitwise OR of the two operands but discards the result. N and Z are updated from the OR result; C and V
are cleared.

\begin{lstlisting}
result = operand1 | operand2
; result is discarded
; N and Z updated from result; C and V cleared
\end{lstlisting}

\textbf{Utility:} Limited. \mnemonic{ORRI} already provides the same flag effects with a result write-back.

\section{Implementation Notes}

\subsection{Current Simulator}

In the current SIRC-VM simulator:

\begin{itemize}
	\item Undocumented instructions are recognized and decoded
	\item The undocumented ALU test-variant opcodes execute following the "test variant" pattern (no write-back)
	\item Flag updates match the corresponding documented operation exactly, as verified by the reference test suite
	\item Behavior may change in future versions
\end{itemize}

\subsection{Hardware Implementation}

In a hardware implementation:

\begin{itemize}
	\item The decoder treats bit 3 = 1 as "don't write back"
	\item ALU operations still execute normally
	\item Flags are still updated
	\item The write-back stage is simply disabled
\end{itemize}

This means undocumented encodings may execute, but their exact behavior depends on the hardware implementation details
and is not part of the architectural contract.

\section{Why Undocumented?}

These instructions were left undocumented for several reasons:

\begin{enumerate}
	\item \textbf{Limited utility}: Most have no practical use beyond documented instructions
	\item \textbf{Incomplete semantics}: The designer hasn't fully defined their behavior
	\item \textbf{Opcode space reservation}: May be used for future features
	\item \textbf{Implementation flexibility}: Allows hardware optimizations
\end{enumerate}

\section{Recommendations}

\subsection{For Application Developers}

\textbf{Do not use undocumented instructions in production code.}

\begin{itemize}
	\item Behavior may change between CPU revisions
	\item Assemblers may reject them or emit warnings
	\item Code may not be portable
	\item Debugging tools may not recognize them
\end{itemize}

\subsection{For Compiler Writers}

\begin{itemize}
	\item Do not generate undocumented instructions
	\item Use only documented instructions for all operations
	\item If optimization requires new operations, request formal documentation
\end{itemize}

\subsection{For Hardware Implementers}

\begin{itemize}
	\item These opcodes may be implemented following the logical pattern
	\item Document any implementation-specific behavior
	\item Consider reserving them for future standardization
\end{itemize}

\subsection{For Researchers/Hackers}

Experimenting with undocumented instructions requires the following precautions:

\begin{itemize}
	\item Test thoroughly on the specific hardware or simulator version in use
	\item Document the findings
	\item Isolate usage in test code, not production
	\item Be prepared for behavior changes
\end{itemize}

\section{Future Standardization}

Some undocumented instructions may be formally specified in future SIRCIS revisions. Candidates for standardization
include:

\begin{itemize}
	\item \textbf{ADC Test (\opcode{09}/\opcode{29}/\opcode{39})}: For carry flag testing in multi-precision
	      arithmetic
	\item \textbf{SBC Test (\opcode{0B}/\opcode{2B}/\opcode{3B})}: For borrow flag testing in multi-precision
	      arithmetic
\end{itemize}

Others (the ADD Test and OR Test variants) will likely remain undocumented or be repurposed.

\section{Opcode Space for Expansion}

The SIRCIS instruction set has limited expansion space:

\begin{itemize}
	\item 14 undocumented opcodes (could be formally specified)
	\item Reserved shift type (111) could enable additional shift modes
	\item The reserved status-update source encoding (AF = 11) could select a new flag-update source
	\item Standard optional coprocessors and model-specific coprocessors can provide extended functionality through the
	      documented coprocessor compatibility rules
\end{itemize}

Any future expansion must maintain backward compatibility with existing code.
